I want to know how the zig compiler implements incremental compilation.

First, run `git clone https://codeberg.org/ziglang/zig` to get the source code.
Then, read the source code and figure out how it implements incremental compilation.
Write the report in `docs/incremental-compilation.md`.
